<% @albums.each do |a|%>
	<%= link_to(a.attributes.title, user_album_photo_index_path(a.attributes.id))  %>
	<%= a.attributes.published_date + a.attributes.summary + a.attributes.link  %>
	<img src="<%= a.attributes.thumbnail %>"/>

<% end %>